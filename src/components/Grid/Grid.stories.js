import React from 'react';

import GridForm from './Grid';

export default {
    title: "Material Ui/Grid",
    component: GridForm,
}

export const GridView = (args) => <GridForm {...args}>GridForm</GridForm>;