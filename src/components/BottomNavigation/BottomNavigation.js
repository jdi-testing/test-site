import React from 'react';
import MBottomNavigation from '@material-ui/core/BottomNavigation';

const BottomNavigation = (props) => <MBottomNavigation {...props} />;

export default BottomNavigation;
